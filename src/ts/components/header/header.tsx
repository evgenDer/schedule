import React from 'react';
import { PageHeader } from 'antd';
import SelectComponent from '../SelectComponent/SelectComponent';

const title: string = 'Schedule'

const Header: typeof PageHeader = () => {
    return (
        <PageHeader title={title}>

        </PageHeader>
    )
}